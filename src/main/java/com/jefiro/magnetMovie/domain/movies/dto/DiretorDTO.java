package com.jefiro.magnetMovie.domain.movies.dto;

public record DiretorDTO(
        String id,
        String name,
        String original_name,
        String profile_path,
        String credit_id,
        String job

        ) {
}
